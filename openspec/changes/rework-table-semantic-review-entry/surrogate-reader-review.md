# Surrogate Reader Review

## Disposition

`ACCEPT` for reader review. This disposition does not set business acceptance, approve the candidate taxonomy, or authorize broader semantic inference.

## Decisive reasons

- The default entry now exposes all 477 tables through one stable catalog and shows `current result / total` plus `loaded / total`; the 2 collaboration groups and 5 investigation cards are explicitly isolated as special-review samples.
- The overview distinguishes mutually exclusive disposition counts from overlapping candidate coverage and states that 397 Unknown and 25 Conflict are evidence states rather than an error rate.
- Table details are assertion-centered: direct evidence, counterevidence, field role, review decision, relations, and unresolved status remain separately visible. Field evidence is attached only to the assertion it assists and is explicitly barred from table-label voting.
- Candidate navigation remains shallow and multi-valued. Missing business context or responsibility is displayed as not formed rather than filled from a closed taxonomy.
- A simulated missing shard exposed `loaded 450 / 477` and the failed shard path instead of presenting the partial catalog as complete.

## Falsification checks

- Positive field-assisted case: `CFG_OTC_CONTR_PRECISION_RULE` shows its configuration candidate, direct evidence, and `RULE_ID` as supporting field evidence under that assertion.
- Ambiguous case: `AUTOCALL_KIDATE_119967` remains Unknown with no invented business classification and shows that fields were not used.
- Misleading-name case: `ADM_UPDATE_AUDIT_LOG` keeps TRS context, operational-log responsibility, and Conflict visible instead of collapsing its name into one definitive class.
- Counterevidence case: `ADM_LEND_INFO_AUDIT_LOG` exposes Conflict and its counterevidence in the table detail.
- Scope-vacuity case: zero human reviews remains visible as zero; a passing engineering Gate is not presented as reader delivery or business confirmation.
- Special-group case: the first candidate collaboration group narrows the matrix to 7 of 477 tables and provides an explicit return to all tables.

## Remaining boundary

The fixed governance Case Pack still pins the previous Stage 4 manifest hash. The Harness correctly reports `HASH_DRIFT` after this intentional regeneration. Updating that authorization snapshot belongs to governance synchronization and is not silently performed by this reader-UI change.

## Default-scope amendment review

Disposition: `ACCEPT` after one wording correction.

- The default matrix uses the Canonical `SUBJECT` disposition and shows 233 / 477; it does not run a second numeric-suffix rule in the browser.
- The remaining 244 tables are reachable as one collapsed scope and retain the separate `LIKELY_VARIANT` (69), `UNKNOWN` (78), and `STANDALONE` (97) outcomes.
- The page explicitly states that a numeric suffix does not prove backup, disuse, or permission to delete. The full 477-table physical inventory and suffix-table deep links remain available.
- A first review found that “业务主体表” overstated what the physical disposition proves. The reader label was corrected to “主体候选”; no Canonical facts or counts changed.
- Raw profile and field-support JSON remains available for audit but is now labeled “审计与调试数据（一般无需查看）” and stays collapsed by default.
