## 1. 读者统计与轻量目录契约

- [ ] 1.1 先补充失败测试，覆盖全量总数、互斥处置分布、可重叠候选覆盖、Unknown/Conflict、字段辅助和人工审阅状态口径
- [ ] 1.2 在生成器中计算带口径类型的全貌统计与各候选值的唯一 `asset_id` 覆盖数
- [ ] 1.3 为每张表生成轻量矩阵行，包含候选值摘要、物理处置、Unknown/Conflict 和字段辅助状态，并验证 477 个稳定 `asset_id` 无丢失

## 2. 全貌、导航与全量矩阵

- [ ] 2.1 先补充页面行为测试，证明首次打开能看到总量、当前范围、加载范围、审阅边界和全量入口
- [ ] 2.2 将默认页面重组为全貌条、浅层表语义导航、全量表矩阵和单表画像，并明确树只用于浏览、候选可多值
- [ ] 2.3 实现上下文、锚点、职责、状态和搜索的统一筛选状态，持续显示“当前结果 / 全量总数”和全部生效条件
- [ ] 2.4 实现清除范围、分页边界、分片加载进度和失败分片提示，确保首批限制或加载失败不会伪装成完整目录

## 3. 单表画像与字段辅助解释

- [ ] 3.1 先补充代表表测试，覆盖直接证据、Counterevidence、竞争/Unknown、Review Decision 空状态和字段 `SUPPORT / DISTINGUISH / COUNTER / NOT_USED`
- [ ] 3.2 按物理事实、上下文、锚点、职责、表组和关系组织单表画像，并以具体 Assertion 为中心关联 Evidence 与 Counterevidence
- [ ] 3.3 将字段辅助作用显示在对应表级 Assertion 下，将未使用字段摘要和方法分数降到折叠技术详情
- [ ] 3.4 保持 Panorama Object Card、hash 深链和多入口复用同一 `asset_id`，并验证返回导航不会丢失当前范围

## 4. 专项复核边界

- [ ] 4.1 先补充页面测试，证明 2 个业务协作组与 5 张调查卡不会被计为全量分类，且每类入口显示用途、成员数和未决状态
- [ ] 4.2 将业务协作组、调查卡、物理变体组和结构邻域迁入独立专项复核区域，并保留返回全量矩阵的明确入口
- [ ] 4.3 保持调查卡关系、成员职责、字段链接和 Unknown/Conflict 可复核，不改变其 Canonical 数据或模型 Gate

## 5. 真实页面验证与代理评审

- [ ] 5.1 使用真实静态页面执行首次加载、候选筛选、清除、分页、专项范围、单表证据和模拟分片失败交互，不能只依赖 HTML 字符串断言
- [ ] 5.2 重生成固定 stage4 表语义审阅产物，核对 477 张表全部可达、统计口径与 Canonical 数据一致且没有字段侧写回
- [ ] 5.3 按项目强制规则执行独立代理读者评审，覆盖正例、模糊案例、名称反例、Unknown/Conflict、字段辅助边界和空通过；有问题则修复并复审
- [ ] 5.4 运行聚焦测试、完整相关测试和 `openspec validate rework-table-semantic-review-entry --strict`，记录 `ACCEPT / REWORK / STOP / DEFER`，但不代替用户业务验收
