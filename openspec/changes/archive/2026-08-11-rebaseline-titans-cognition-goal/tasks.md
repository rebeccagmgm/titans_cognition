## 1. 建立当前状态基线

- [x] 1.1 盘点 README、CONTEXT、相关 `docs/spec` 模块、案例记录和自动化输出中的完成声明，并标出它们各自依赖的证据类型。
- [x] 1.2 按 `physical_extraction`、`structural_cognition`、`reader_delivery`、`business_acceptance`、`scale_authorization` 五个维度记录当前状态、已确认事实和未满足条件。

## 2. 对齐项目目标与状态口径

- [x] 2.1 更新 README 和 CONTEXT，将“可下钻的 TITANS 业务全貌”确立为唯一交付目标，并明确当前成果只覆盖到哪些状态维度。
- [x] 2.2 更新相关 `docs/spec` 模块，使技术阶段、读者交付、业务验收和规模化授权不再互相替代，并保持与本 Change 的治理规则一致。
- [x] 2.3 更新当前案例、评审和度量说明，将现有 Gold Set、11/11 结果和 Gate B 明确定级为结构规则回归证据，而非业务全貌验收或 V1C 授权。
- [x] 2.4 保留既有事实数据、测试结果和历史输出原文；对已失效的结论添加重新解释与当前状态说明，不追溯改写历史记录。

## 3. 审核自动化状态输出

- [x] 3.1 检查 Gate B、`v1c_authorized` 及相关 CLI 或评估输出，确认它们是否会把工程检查通过误报为业务验收或规模化授权。
- [x] 3.2 仅对与新治理契约冲突的自动化状态逻辑做最小修正，并补充覆盖授权边界和失败保留的契约测试。

## 4. 验证重新定基线结果

- [x] 4.1 运行现有测试集，确认口径调整没有破坏确定性提取、规则回归和历史结果读取。
- [x] 4.2 运行 OpenSpec 严格校验，确认 proposal、spec、design 和 tasks 相互一致且无未解决的实施前决策。
- [x] 4.3 复核最终变更范围，确认未接入新证据源、未调用模型、未生成全量业务地图、未启动 V1C 推理。
- [x] 4.4 向用户呈现五维状态基线、仍缺失的业务交付条件和后续 Change 边界，供目标与状态验收。
