## 1. 范围与最小契约

- [x] 1.1 为配置驱动的 Schema/对象过滤、首轮 233/244 对账和范围漂移编写测试。
- [x] 1.2 定义并测试 `concepts.jsonl`、`field_concept_links.jsonl`、`manifest.json` 的最小 Schema。
- [x] 1.3 增加独立 `discover-field-concepts` 命令和 TRADEFLOW 首轮配置，不修改历史 Physical Facts。

## 2. 字段表示与相似召回

- [x] 2.1 编写字段名切分、中文规范化、缺失注释、同名异注释、异名同注释和类型差异 Fixture。
- [x] 2.2 实现名称、注释、类型和低权重表上下文的组合字段表示。
- [x] 2.3 使用版本锁定的成熟本地算法完成字符 n-gram、TF-IDF/余弦 Top-K 召回，并测试通用词降权、阈值和防二次方爆炸的候选安全上限。

## 3. 三级概念树

- [x] 3.1 为 `金额 → 名义本金 → 初始/当前名义本金`、同名异义和未分配字段编写层次聚类测试。
- [x] 3.2 实现有阈值的层次聚类和最多三级的父子概念生成，禁止连通分量式链式误并。
- [x] 3.3 实现基于组内中文短语、英文 Token 和父子差异的候选标签；无法形成受支持簇的字段保持未分配。

## 4. 输出与审阅

- [x] 4.1 写出三个正式结果文件、可选诊断文件和字段总量/未分配对账统计。
- [x] 4.2 生成按需渲染的审阅页，支持概念分页下钻、字段/表分页和后台字段反查。
- [x] 4.3 增加重复运行稳定性、Manifest、CLI、输出 Schema、静态反查页面和大规模 DOM 上限测试。

## 5. 首轮试验

- [x] 5.1 在 LLM 禁用模式下对 TRADEFLOW 233 张表运行一次并记录运行规模、候选数、概念数、未分配数和候选安全上限是否造成截断。
- [x] 5.2 审阅“名义本金”概念路径、相关字段/表以及 `AMOUNT/金额` 明显误并，形成简短可见结果和问题清单。
- [x] 5.3 只有试验表现出增量导航价值时才提出下一轮调参、人工修正或第二 Schema 验证；否则停止扩展并诚实报告失败。
